pre-commit:
  commands:
    deadnix:
      run: deadnix --edit .
      stage_fixed: true

pre-push:
  commands:
    validate:
      run: nix flake check --all-systems
    deadnix:
      run: deadnix .
